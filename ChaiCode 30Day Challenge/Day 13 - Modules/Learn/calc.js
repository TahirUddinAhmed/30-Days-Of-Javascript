// calculation 
// Named Export
export const sum = (a, b) => {
    return a + b;
}

export const sub = (a, b) => {
    return a - b;
}

const mul = (a, b) => {
    return a * b;
}

const div = (a, b) => {
    return a / b;
}

// another way to export 
export { mul, div };